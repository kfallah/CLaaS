# =============================================================================
# CLaaS Docker Compose — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the required values:
#   cp .env.example .env
#
# Only TELEGRAM_BOT_TOKEN is required. Everything else has sensible defaults.
# =============================================================================

# REQUIRED — Get yours from https://t.me/BotFather
TELEGRAM_BOT_TOKEN=

# OPTIONAL — Hugging Face token (needed only for gated models)
# HF_TOKEN=

# OPTIONAL — Model configuration
# MODEL=Qwen/Qwen3-8B
# MAX_MODEL_LEN=32768
# GPU_MEMORY_UTILIZATION=0.90

# OPTIONAL — LoRA identity
# LORA_NAME=openclaw/assistant

# OPTIONAL — CLaaS API URL (for feedback plugin; default http://claas-api:8080 in Docker)
# CLAAS_API_URL=http://claas-api:8080

# OPTIONAL — Port mapping (host ports)
# CLAAS_API_PORT=8080
# OPENCLAW_PORT=18789
