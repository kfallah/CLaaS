# =============================================================================
# CLaaS Docker Compose — Environment Configuration
# =============================================================================
# Copy to .env:  cp .env.example .env
#
# Run with a profile:
#   docker compose --profile local up --build    # GPU required
#   docker compose --profile tinker up --build   # no GPU needed
# =============================================================================

# REQUIRED — Get yours from https://t.me/BotFather
TELEGRAM_BOT_TOKEN=

# ── Tinker profile only ──────────────────────────────────────────
# TINKER_API_KEY=your-tinker-api-key-here

# ── Local profile only ──────────────────────────────────────────
# HF_TOKEN=
# MAX_MODEL_LEN=32768
# GPU_MEMORY_UTILIZATION=0.70

# ── Shared ───────────────────────────────────────────────────────
# MODEL=Qwen/Qwen3-8B                  # local default
# MODEL=gpt-oss/GPT-OSS-120B              # tinker default
# LORA_NAME=openclaw/assistant
# CLAAS_API_PORT=8080
# OPENCLAW_PORT=18789
# TINKER_PROXY_PORT=8000
