FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app
COPY . /app

RUN pip install --no-cache-dir . uvicorn

EXPOSE 8000

ENTRYPOINT ["uvicorn", "claas.tinker_inference_proxy:app", "--host", "0.0.0.0", "--port", "8000"]
